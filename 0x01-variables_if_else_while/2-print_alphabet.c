#include <stdio.h>

/**
 * writing a program that print alphabet in lower case
 *follow by a new lin
 */
 int main()
{
	char alp;
	for (alp = 'a'; alp <= 'z'; alp++)
		putchar(alp);

	putchar('\n');

	return 0;
}
